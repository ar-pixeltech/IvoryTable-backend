const express = require('express')
const router = express.Router()
// const orderController = require('../controllers/order.controller')
// const auth = require('../middlewares/auth.middleware')

// router.post('/', auth, orderController.createOrder)
// router.get('/', auth, orderController.getOrders)
// router.get('/:id', auth, orderController.getOrderById)

// router.post('/:id/items', auth, orderController.addItem)
// router.put('/:id/items/:itemId', auth, orderController.updateItem)
// router.delete('/:id/items/:itemId', auth, orderController.removeItem)

// router.post('/:id/apply-discount', auth, orderController.applyDiscount)
// router.post('/:id/close', auth, orderController.closeOrder)
// router.post('/:id/cancel', auth, orderController.cancelOrder)

module.exports = router
